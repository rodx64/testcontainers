spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ems?useSSL=false
    username: root
    password: docker
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true